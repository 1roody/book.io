name: Pipeline

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build-application:
    name: Build Application
    uses: ./.github/workflows/build.yml
    with:
      push-image: true
